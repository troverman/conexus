#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../geojsonhint/bin/geojsonhint" "$@"
  ret=$?
else 
  node  "$basedir/../geojsonhint/bin/geojsonhint" "$@"
  ret=$?
fi
exit $ret
